Generate expectations for the following Effect pattern:

Pattern Name: ${prompt.patternName}
Pattern Content: ${prompt.patternContent}
Execution Result: ${prompt.executionResult}
Expected Output: ${prompt.expectedOutput}
Actual Output: ${prompt.actualOutput}

Please provide:
1. Expected behavior based on the pattern
2. Discrepancies between expected and actual behavior
3. Recommendations for fixing any issues

Return a JSON object with the structure:
{
  "expectations": ["array of expected behaviors"],
  "discrepancies": ["array of discrepancies"],
  "recommendations": ["array of recommendations"]
}