---
provider: google
model: gemini-2.5-flash
parameters:
  temperature: 0.3
  max_tokens: 800
  response_format: 
    type: json_object
---

# Object Generation Test Prompt

This prompt tests the LLM service's ability to generate structured JSON objects.

## Required Output Format

Please generate a JSON object with the following structure:

```json
{
  "serviceTest": {
    "status": "success",
    "timestamp": "2024-01-01T00:00:00Z",
    "provider": "google",
    "model": "gemini-2.5-flash",
    "testResults": {
      "textGeneration": {
        "passed": true,
        "score": 95,
        "notes": ["Fast response", "Accurate content"]
      },
      "objectGeneration": {
        "passed": true,
        "score": 98,
        "notes": ["Perfect JSON structure", "All fields present"]
      },
      "streaming": {
        "passed": true,
        "score": 92,
        "notes": ["Smooth streaming", "No interruptions"]
      }
    },
    "metadata": {
      "durationMs": 1234,
      "tokenCount": 567,
      "costEstimate": 0.0089
    }
  },
  "codeExample": {
    "language": "typescript",
    "content": "async function testLLMService() {\n  const result = await generateObject({\n    prompt: 'Test prompt',\n    schema: testSchema,\n    provider: 'google',\n    model: 'gemini-2.5-flash'\n  });\n  return result;\n}",
    "description": "Example usage of the LLM service"
  },
  "recommendations": [
    {
      "category": "performance",
      "suggestion": "Consider implementing caching for repeated requests",
      "priority": "medium"
    },
    {
      "category": "error_handling",
      "suggestion": "Add retry logic with exponential backoff",
      "priority": "high"
    },
    {
      "category": "monitoring",
      "suggestion": "Implement comprehensive logging and metrics",
      "priority": "high"
    }
  ]
}
```

## Validation Requirements

1. **All required fields must be present**
2. **Data types must match specification**
3. **Nested objects must maintain structure**
4. **Arrays must contain valid items**
5. **String formats must be correct (dates, etc.)**

## Additional Test Data

Include a nested object structure that demonstrates:
- Deep nesting (at least 3 levels)
- Array handling
- Mixed data types
- Optional fields handling

## Error Handling

The response should include error handling information:
- Common error types
- Recovery strategies
- Prevention measures
